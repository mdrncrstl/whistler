# Project workflow

- After completing an authorized code change, run the appropriate checks, commit the change, and push the current project branch automatically.
- Keep changes local only when the user explicitly says not to commit or push.
