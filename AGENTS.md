# Project workflow

- After completing an authorized code change, run the appropriate checks, commit the change, push the current project branch, and deploy the resulting commit to Vercel production automatically.
- Keep changes local only when the user explicitly says not to commit, push, or deploy.
